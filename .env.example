# Supabase Configuration
# Estas son variables públicas que están diseñadas para ser expuestas en el cliente
# La ANON_KEY es segura porque las políticas RLS (Row Level Security) protegen los datos

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Nota: Estas variables son públicas por diseño (NEXT_PUBLIC_*)
# La seguridad se maneja a través de:
# 1. Row Level Security (RLS) en Supabase
# 2. Middleware de autenticación en Next.js
# 3. Verificación de permisos de admin en el backend
